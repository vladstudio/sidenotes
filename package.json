{
  "name": "vladstudio-sidenotes",
  "displayName": "Sidenotes",
  "description": "A simple VSCode extension to manage and view notes",
  "version": "0.0.1",
  "engines": {
    "vscode": "^1.92.0"
  },
  "categories": [
    "Other"
  ],
  "activationEvents": [
    "onView:sidenotes-sidebar"
  ],
  "main": "./extension.js",
  "contributes": {
    "commands": [
      {
        "command": "sidenotes.refreshSidebar",
        "title": "Refresh Sidenotes"
      },
      {
        "command": "sidenotes.createNewNote",
        "title": "Create New Note",
        "icon": "$(add)"
      }
    ],
    "viewsContainers": {
      "activitybar": [
        {
          "id": "sidenotes-sidebar",
          "title": "Sidenotes",
          "icon": "resources/sidenotes-icon.svg"
        }
      ]
    },
    "views": {
      "sidenotes-sidebar": [
        {
          "id": "sidenotes-files",
          "name": "Sidenotes Files"
        }
      ]
    },
    "menus": {
      "view/title": [
        {
          "command": "sidenotes.createNewNote",
          "when": "view == sidenotes-files",
          "group": "navigation"
        }
      ]
    }
  },
  "scripts": {
    "lint": "eslint .",
    "pretest": "npm run lint",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.92.0",
    "@types/mocha": "^10.0.7",
    "@types/node": "20.x",
    "eslint": "^8.57.0",
    "typescript": "^5.4.5",
    "@vscode/test-cli": "^0.0.9",
    "@vscode/test-electron": "^2.4.0"
  }
}
